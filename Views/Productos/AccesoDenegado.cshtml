@model dynamic

@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-10 mx-auto text-center">
            <h1 class="text-danger mb-3">
                <i class="fas fa-ban fa-2x"></i>
                <br />
                Acceso Denegado (Error 403)
            </h1>
            <hr />

            <h2 class="mb-4">Lo sentimos.</h2>
            
            <p class="lead">
                Su rol de <strong>Cliente</strong> no tiene permisos para acceder a la gestión de productos.
            </p>
            
            <p>
                Esta sección está reservada para usuarios con el rol de <strong>Administrador</strong>.
                Los clientes solo pueden visualizar el catálogo de productos a través del listado general.
            </p>

            <div class="alert alert-info mt-4" role="alert">
                <i class="fas fa-info-circle"></i>
                <strong>Información:</strong> Si cree que es un error, contacte con el administrador del sistema.
            </div>

            <div class="mt-5">
                <a asp-action="Index" asp-controller="Productos" class="btn btn-primary btn-lg me-2">
                    <i class="fas fa-list"></i> Ver Catálogo de Productos
                </a>
                <a asp-controller="Login" asp-action="Logout" class="btn btn-secondary btn-lg">
                    <i class="fas fa-sign-out-alt"></i> Cerrar Sesión
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    .container {
        min-height: 60vh;
        display: flex;
        align-items: center;
    }
</style>
